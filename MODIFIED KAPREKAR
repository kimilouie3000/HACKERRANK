using System;
using System.Collections.Generic;

class Solution {
    static void kaprekarNumbers(int p, int q) {
        List<int> results = new List<int>();

        for (int n = p; n <= q; n++) {
            if (IsKaprekar(n)) {
                results.Add(n);
            }
        }

        if (results.Count == 0) {
            Console.WriteLine("INVALID RANGE");
        } else {
            Console.WriteLine(string.Join(" ", results));
        }
    }

    static bool IsKaprekar(int n) {
        long square = (long)n * n;
        string sqStr = square.ToString();
        int d = n.ToString().Length;

        string leftStr = sqStr.Length > d ? sqStr.Substring(0, sqStr.Length - d) : "0";
        string rightStr = sqStr.Substring(sqStr.Length - d);

        int left = int.Parse(leftStr);
        int right = int.Parse(rightStr);

        return (left + right) == n;
    }

    static void Main(string[] args) {
        int p = int.Parse(Console.ReadLine());
        int q = int.Parse(Console.ReadLine());

        kaprekarNumbers(p, q);
    }
}
